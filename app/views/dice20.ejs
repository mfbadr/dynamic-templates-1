<!doctype html>
<html>
  <head>
    <meta charset="utf-8">
    <title>d20 Roller</title>
    <link rel="stylesheet" href="/css/dice.css">
  </head>
  <body>
  <h1>Rolling <%= rolls %> d20</h1>
    <% var sum = 0 %>
    <% for( var i = 0; i < rolls; i++) { %> 
      <% var thisRoll = Math.floor(Math.random() * 20) + 1 %>
        <% sum += thisRoll %>
        <% var filePath = "/images/dice/twenty" + thisRoll + ".jpeg" %>
        <img src="<%= filePath %>">
    <% } %>
    <h2> The sum is <%= sum %> </h2>
  </body>
</html>
